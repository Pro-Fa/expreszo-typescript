/**
 * `@pro-fa/expreszo/mcp-server` entry — MCP (Model Context Protocol) server
 * wrapping the expreszo language service. Exports `createMcpServer` for
 * programmatic use; the CLI entry lives at `dist/bin/mcp-server.mjs`.
 */
export * from '../mcp-server/index.js';
